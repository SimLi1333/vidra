---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: infrahubresources.infrahub.operators.com
spec:
  group: infrahub.operators.com
  names:
    kind: InfrahubResource
    listKind: InfrahubResourceList
    plural: infrahubresources
    singular: infrahubresource
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: InfrahubResource is the Schema for the infrahubresources API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: InfrahubResourceSpec defines the desired state of InfrahubResource
            properties:
              destination:
                description: Destination contains the destination information for
                  the resource
                properties:
                  namespace:
                    description: Namespace in the Kubernetes cluster where the resource
                      should be sent
                    type: string
                  server:
                    description: Server URL for the destination (usually a Kubernetes
                      API endpoint)
                    type: string
                type: object
              ids:
                description: IDs contains important identifiers for the resource
                properties:
                  artefactID:
                    description: Unique identifier for the artifact
                    type: string
                  checksum:
                    description: Checksum of the artifact
                    type: string
                  storageID:
                    description: Storage ID for the artifact
                    type: string
                required:
                - artefactID
                - checksum
                - storageID
                type: object
              source:
                description: Source contains the source information for the Infrahub
                  API interaction
                properties:
                  artefactName:
                    description: Artifact name that is being handled
                    minLength: 1
                    type: string
                  infrahubAPIURL:
                    description: URL for the Infrahub API
                    pattern: ^(http|https)://[a-zA-Z0-9.-]+(:[0-9]+)?(?:/[a-zA-Z0-9-]+)*$
                    type: string
                  targetBranch:
                    description: The target branch to interact with
                    minLength: 1
                    type: string
                  targetDate:
                    description: The target date for the request
                    format: date-time
                    type: string
                required:
                - artefactName
                - infrahubAPIURL
                - targetBranch
                type: object
            required:
            - source
            type: object
          status:
            description: InfrahubResourceStatus defines the observed state of InfrahubResource
            properties:
              DeployState:
                enum:
                - Pending
                - Running
                - Succeeded
                - Failed
                - Stale
                type: string
              checksum:
                description: |-
                  INSERT ADDITIONAL STATUS FIELD - define observed state of cluster
                  Important: Run "make" to regenerate code after modifying this file
                type: string
              lastError:
                type: string
              lastSyncTime:
                format: date-time
                type: string
              managedResources:
                items:
                  properties:
                    apiVersion:
                      type: string
                    kind:
                      type: string
                    name:
                      type: string
                    namespace:
                      type: string
                  required:
                  - apiVersion
                  - kind
                  - name
                  type: object
                type: array
              manifests:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
